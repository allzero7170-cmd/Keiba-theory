# Keiba-theory

競馬理論の分析メモを作成するための、日本語UIのシンプルなWebアプリです。  
最小版として、HTML/CSS/JavaScriptだけで動作し、入力データはブラウザ内のローカルストレージに保存します。

## 機能

- 日付、競馬場、レース番号、レース名、出走頭数の入力
- コンピ順位1位〜10位の馬番・馬名とコンピ指数の入力
- 出走頭数×コンピ順位の基準値表を使った階級指数の自動計算
- テクニカル6のパターンとメモの入力
- 調教評価、朝イチ単勝順位、朝イチ複勝順位、馬連順位の入力
- 軸候補、相手候補、買い目メモの入力と保存済みメモ表示
- スマホ・タブレットで見やすい縦長レイアウト
- ブラウザ内保存（localStorage）

## 階級指数

```text
階級指数 = コンピ指数値 - 基準値
```

基準値は `docs/kaikyuu-index.md` の「階級分け基準値」を使います。  
プラス幅が大きいほど、そのコンピ順位帯ではお買い得度が高い馬として扱います。

## 使い方

1. `index.html` をブラウザで開きます。
2. レース情報、出走頭数、コンピ順位、コンピ指数を入力します。
3. 階級指数が自動で表示されます。
4. テクニカル6、補助指標、結論メモを入力します。
5. 「保存する」を押すと、ブラウザ内にデータが保存され、右下または下部の「保存済みメモ」に反映されます。
6. 「入力をクリア」を押すと、画面上の入力欄だけを空にできます。保存済みデータは削除されません。
7. 「保存データ削除」を押すと、ブラウザ内に保存されたデータも削除されます。

## ファイル構成

```text
.
├── README.md
├── index.html
├── styles.css
├── app.js
└── docs/
    └── kaikyuu-index.md
```

## 開発メモ

現時点では外部ライブラリやビルドツールを使っていません。今後、複数レース保存、買い目の自動候補生成、CSV入出力、画像読み取りなどを追加しやすい構成にしていく予定です。
